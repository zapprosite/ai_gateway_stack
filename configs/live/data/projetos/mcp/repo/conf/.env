PORT_API=8010
PORT_LTL=4000
PORT_OLLAMA=11434
PORT_QDRANT=6333

# Persistent model/cache directories on host
OLLAMA_MODELS=/data/llm/ollama_models
HF_HOME=/data/hf

# GPU settings (if NVIDIA GPU is available)
NVIDIA_VISIBLE_DEVICES=all
NVIDIA_DRIVER_CAPABILITIES=compute,utility

# Optional: compose profiles (comma-separated). Leave empty to disable sidecars.
# COMPOSE_PROFILES=pg,deepwiki,zen

